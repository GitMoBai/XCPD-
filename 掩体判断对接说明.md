# MoBai 掩体判断对接说明

## 1. 实现思路

当前实现使用 Unity `Physics.Raycast` 做头、胸、腿三点射线：

- 真人和人机统一调用 `获取眼睛位置()`，但内部保持两套运行时逻辑：真人读取 `ReadRoleBone(role, 0x540)`，人机读取自身 Animator 的 Head；骨骼不可用时均尝试从各自 Collider 的世界 Bounds 顶部取得眼睛位置。
- 真人和人机分别调用 `生成真人掩体检测点()`、`生成人机掩体检测点()`。胸、腿优先由各自 Collider Bounds 动态生成；Collider 不可用时，按根节点到动态眼睛的实际高度比例生成，不再使用 `1.35/1.55` 等固定世界高度。
- 方向为 `(终点 - 起点) / 距离`，最大射线距离为起终点间距离。
- LayerMask 只排除本地玩家实际所在层：`~(1 << localPlayerLayer)`。
- `QueryTriggerInteraction` 传 `0`（`UseGlobal`）。
- 射线没有命中则视为可见；命中后读取 `RaycastHit.collider`，若它属于目标的子层级 Collider 集合则视为命中目标，否则才视为场景遮挡。不再使用固定 `1.0m` 距离容差。
- 每轮固定完成头、胸、腿三条射线，不允许以单条结果提前结束；任一点可见即判定目标可见，三个点全部被挡才算本轮遮挡，不做命中人物碰撞体后的续射。
- 三点持续全部被挡满 80ms 才正式判定遮挡；任一轮存在可见点会立即清除遮挡状态和计时，判定与帧率无关。
- 普通自瞄、普通子弹追踪使用该结果；魔法子弹必须走独立目标链，不读取此结果。

## 2. 为什么要用请求队列

外置 Overlay 线程不能安全地直接调用 Unity Physics。因此：

1. Overlay/目标扫描线程调用 `掩体查询()`，只提交本人/目标地址、根坐标和目标运行时类型。
2. 游戏主线程每帧调用 `处理请求()`，在同一安全线程内读取真人骨骼、人机 Animator、Collider Bounds，生成头胸腿三点并执行 `Physics.Raycast`。
3. 结果按实体地址缓存，缓存有效期为 250ms。
4. 同一实体已有请求排队时不更新其坐标；队列最多保留 128 个请求。
5. 尚无结果或结果过期时默认返回 `false`（暂按未遮挡处理）。

内绘模式如果 `Present` 确实运行在可调用 Unity API 的游戏渲染线程，也可以在 `Present` 中直接调用 `处理请求()`；外绘模式应 Hook 游戏内组件的 `Update/LateUpdate` 来处理。

## 3. 公共声明与队列

```cpp
#include <cstdint>
#include <array>
#include <mutex>
#include <unordered_map>
#include <vector>
#include <Windows.h>
#include <glm/glm.hpp>

bool 存在掩体(const glm::vec3& 起点, const glm::vec3& 终点);

namespace 掩体共享 {
    using 检测点列表 = std::array<glm::vec3, 3>;

    struct 射线请求 {
        uint64_t id;       // 使用稳定且唯一的实体地址
        uintptr_t 本人地址;
        glm::vec3 本人根坐标;
        uintptr_t 目标地址;
        glm::vec3 目标根坐标;
        掩体角色类型 目标类型;
        bool 执行射线;
    };

    struct 掩体结果 {
        bool 遮挡 = false;
        ULONGLONG 更新时间 = 0;
        ULONGLONG 遮挡开始时间 = 0;
        glm::vec3 目标眼睛位置 = {};
        bool 目标眼睛有效 = false;
    };

    inline std::mutex 互斥;
    inline std::vector<射线请求> 请求队列;
    inline std::unordered_map<uint64_t, 掩体结果> 结果表;

    bool 掩体查询(uint64_t id,
                  uintptr_t 本人地址, const glm::vec3& 本人根坐标,
                  uintptr_t 目标地址, const glm::vec3& 目标根坐标,
                  掩体角色类型 目标类型, bool 执行射线,
                  glm::vec3* 目标眼睛位置 = nullptr,
                  bool* 目标眼睛有效 = nullptr);

    void 处理请求();
}
```

## 4. Raycast 解析与遮挡判定

以下代码依赖 MoBai 使用的 `UnityResolve`。对接方如果使用其他 IL2CPP 解析库，需要替换 `Get`、`Method`、`Cast`、`Invoke`，但必须保持六参数重载、函数签名和 `RaycastHit` 内存布局一致。

```cpp
using 掩体射线函数 = UnityResolve::MethodPointer<
    bool,
    glm::vec3,
    glm::vec3,
    UnityResolve::UnityType::RaycastHit&,
    float,
    int,
    int>;

static bool 执行掩体射线(
    掩体射线函数 function,
    const glm::vec3& origin,
    const glm::vec3& direction,
    UnityResolve::UnityType::RaycastHit& hit,
    float distance,
    int layerMask) {
    __try {
        // 最后一项：QueryTriggerInteraction.UseGlobal
        return function(origin, direction, hit, distance, layerMask, 0);
    }
    __except (EXCEPTION_EXECUTE_HANDLER) {
        return false;
    }
}

static int 获取本地玩家层() {
    static int layer = -1;
    static ULONGLONG nextAttempt = 0;

    if (layer > 0 && layer < 32)
        return layer;

    const ULONGLONG now = GetTickCount64();
    if (now < nextAttempt)
        return -1;
    nextAttempt = now + 3000;

    try {
        if (!本人数据::本人地址)
            return -1;

        auto* component = reinterpret_cast<UnityResolve::UnityType::Component*>(
            本人数据::本人地址);
        auto* gameObject = component->GetGameObject();
        auto* coreAssembly = UnityResolve::Get("UnityEngine.CoreModule.dll");
        auto* gameObjectClass = coreAssembly
            ? coreAssembly->Get("GameObject") : nullptr;
        auto* getLayer = gameObjectClass
            ? gameObjectClass->Get<UnityResolve::Method>("get_layer") : nullptr;

        if (gameObject && getLayer) {
            const int value = getLayer->Invoke<int>(gameObject);
            if (value > 0 && value < 32)
                layer = value;
        }
    }
    catch (...) {}

    return layer;
}

static int 获取掩体层遮罩() {
    const int playerLayer = 获取本地玩家层();
    return playerLayer > 0 && playerLayer < 32
        ? ~(1 << playerLayer)
        : ~0;
}

bool 存在掩体(const glm::vec3& 起点, const glm::vec3& 终点) {
    if (!std::isfinite(起点.x) || !std::isfinite(起点.y) ||
        !std::isfinite(起点.z) || !std::isfinite(终点.x) ||
        !std::isfinite(终点.y) || !std::isfinite(终点.z))
        return false;

    glm::vec3 direction = 终点 - 起点;
    const float distance = glm::length(direction);
    if (!std::isfinite(distance) || distance < 1.0f)
        return false;
    direction /= distance;

    static 掩体射线函数 raycast = nullptr;
    static ULONGLONG nextResolveAttempt = 0;
    const ULONGLONG now = GetTickCount64();

    if (!raycast && now >= nextResolveAttempt) {
        nextResolveAttempt = now + 2000;

        auto assembly = UnityResolve::Get("UnityEngine.PhysicsModule.dll");
        auto physics = assembly ? assembly->Get("Physics") : nullptr;
        if (physics) {
            for (auto method : physics->methods) {
                if (!method || method->name != "Raycast" ||
                    method->args.size() != 6)
                    continue;

                bool valid = true;
                for (int i = 0; i < 4; ++i)
                    valid = valid && method->args[i] && method->args[i]->pType;
                if (!valid)
                    continue;

                const std::string& t0 = method->args[0]->pType->name;
                const std::string& t1 = method->args[1]->pType->name;
                const std::string& t2 = method->args[2]->pType->name;
                const std::string& t3 = method->args[3]->pType->name;

                if (t0.find("Vector3") != std::string::npos &&
                    t1.find("Vector3") != std::string::npos &&
                    t2.find("RaycastHit") != std::string::npos &&
                    t3.find("Single") != std::string::npos) {
                    raycast = method->Cast<
                        bool,
                        glm::vec3,
                        glm::vec3,
                        UnityResolve::UnityType::RaycastHit&,
                        float,
                        int,
                        int>();
                    break;
                }
            }
        }
    }

    if (!raycast)
        return false;

    const int sceneMask = 获取掩体层遮罩();
    UnityResolve::UnityType::RaycastHit hit{};
    const bool hasHit = 执行掩体射线(
        raycast, 起点, direction, hit, distance, sceneMask);
    const float distanceToTarget = hasHit
        ? glm::length(hit.m_Point - 终点)
        : -1.0f;

    return hasHit && distanceToTarget >= 1.0f;
}
```

对应的 `RaycastHit` 布局为：

```cpp
struct RaycastHit {
    glm::vec3 m_Point;
    glm::vec3 m_Normal;
    uint32_t m_FaceID;
    float m_Distance;
    glm::vec2 m_UV;
    uint32_t m_Collider;
};
```

## 5. 主线程处理函数

```cpp
void 掩体共享::处理请求() {
    std::vector<射线请求> batch;
    {
        std::lock_guard<std::mutex> lock(互斥);
        batch.swap(请求队列);
    }

    const ULONGLONG now = GetTickCount64();
    for (const auto& request : batch) {
        // 下列 Unity 调用全部留在游戏线程：
        // 1. 获取本人眼睛；2. 按真人/人机生成三点；3. 收集目标全部子 Collider。
        const bool geometryValid = 生成动态取点与碰撞体列表(request, points, targetColliders);
        unsigned char blockedPointCount = 0;
        if (request.执行射线 && geometryValid) {
            for (const auto& 终点 : points) {
                if (目标点被场景遮挡(origin, 终点, targetColliders))
                    ++blockedPointCount;
            }
        }
        const bool blocked = request.执行射线 && geometryValid &&
                             blockedPointCount == points.size();
        std::lock_guard<std::mutex> lock(互斥);
        auto& result = 结果表[request.id];
        if (blocked) {
            if (result.遮挡开始时间 == 0) result.遮挡开始时间 = now;
            result.遮挡 = now - result.遮挡开始时间 >= 80;
        } else {
            result.遮挡开始时间 = 0;
            result.遮挡 = false;
        }
        result.更新时间 = now;
    }

    static ULONGLONG lastCleanup = 0;
    if (now - lastCleanup >= 5000) {
        lastCleanup = now;
        std::lock_guard<std::mutex> lock(互斥);
        for (auto it = 结果表.begin(); it != 结果表.end();) {
            if (now - it->second.更新时间 > 5000)
                it = 结果表.erase(it);
            else
                ++it;
        }
    }
}
```

主线程入口示例：

```cpp
void CameraUpdateHook(void* instance) {
    掩体共享::处理请求();
    CallOriginalCameraUpdate(instance);
}
```

MoBai 当前从 `Assembly-CSharp.dll -> CameraController` 中寻找无参数、非静态的 `LateUpdate`，找不到再尝试 `Update`，然后用硬件断点 Hook。对接方不必使用相同 Hook 方案，只要保证 `处理请求()` 在游戏主线程调用即可。

## 6. 目标侧调用

真人：

```cpp
const bool blocked = 掩体共享::掩体查询(
    static_cast<uint64_t>(targetPlayer),
    localPlayer, localPosition,
    targetPlayer, targetPosition,
    掩体角色类型::真人, true);

if (!enableCoverCheck || !blocked) {
    // 加入普通自瞄/普通子弹追踪候选
}
```

人机：

```cpp
glm::vec3 aiEye{};
bool aiEyeValid = false;
const bool blocked = 掩体共享::掩体查询(
    static_cast<uint64_t>(aiAddress),
    localPlayer, localPosition,
    aiAddress, aiPosition,
    掩体角色类型::人机, enableCoverCheck,
    &aiEye, &aiEyeValid);
```

## 7. 对接时必须核对

- 目标是 IL2CPP 版本，并且 `UnityResolve` 已成功初始化到 `GameAssembly.dll`。
- `Physics.Raycast` 必须是六参数重载；不要按名称直接取第一个同名方法。
- 对接方的 `Vector3` 传参 ABI、调用约定及 `RaycastHit` 布局必须与目标 Unity 版本匹配。
- 真人骨骼、人机 Animator、Collider Bounds 和实际 Raycast 都只能在安全的游戏线程执行，不能从 Overlay 工作线程直接调用 Unity API。
- `本人地址` 必须能当作 Unity `Component` 获取 `GameObject`；若对接方保存的是逻辑对象而非组件，应改为从其实际 `GameObject/Component` 取 layer。
- 必须收集目标全部子 Collider；Raycast 命中其中任一个即视为成功到达目标，只有命中非目标 Collider 才算场景遮挡。
- 结果缓存键必须用稳定实体标识；当前直接使用实体地址。
- 当前默认失败开放：解析失败、异常、非法坐标、短距离、无缓存均返回未遮挡。若对接方希望失败关闭，需要自行调整，但会造成初始化期间无法选中目标。
