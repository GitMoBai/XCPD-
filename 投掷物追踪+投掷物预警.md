# 投掷物预警 + 投掷物追踪 —— 移植文档

> 来源：MoBai 项目 `MoBai/Class/Other/Other.h`（`namespace 投掷物预警`，第 95–375 行）
> 与 `MoBai/Class/Other/Other.cpp`（第 2332–3400 行）。
> 下面的代码是**逐字摘出来的**，可以直接贴；注释保留了原样，因为里面记的都是踩过的坑，
> 移植时最容易重新踩一遍。

---

## 0. 这套东西分两个功能，共用一条数据链

| 功能 | 干什么 | 关键开关 |
|---|---|---|
| **投掷物预警** | 把场上飞行中的投掷物画出来（本体点 + 名 + 距我 + 剩余秒），推算落点并在地上画透视圈；落点贴脸时屏幕中央弹「小心 XX！」大字 | `投掷物预警::开关` |
| **投掷物追踪** | 自己刚扔出去的雷，在**飞行途中每帧改写刚体速度**，把它拐向当前自瞄/魔法子弹的目标 | `投掷物预警::追踪开关` |

两者**互不依赖**：关掉预警（甚至关掉某一种雷的逐项预警）不影响追踪，反之亦然。

---

## 1. 线程模型 —— 移植前必须先理解

**IL2CPP 的枚举和读坐标调用只能在游戏主线程做**，渲染线程碰不得。所以是两段式：

```
游戏主线程（每帧 · 由你项目自己的主线程钩子驱动）
    └── 投掷物预警::主线程采样()
          ├─ 每 200ms 枚举一次场上挂 Rigidbody 的雷 → 维护 跟踪 表
          ├─ 选目标 → 执行追踪（写刚体速度）
          └─ 每帧差分速度 / 估重力 / 解落点 → 结果塞进 快照（锁保护）

渲染线程（每帧）
    └── 投掷物预警::绘制()
          ├─ 持锁拷贝一份 快照，**立刻放锁**
          └─ 锁外投影到屏幕画
```

两条铁律：

1. **跨线程只传 POD**。`雷达` 是纯 POD（`char 名[24]` 而不是 `std::string`），跨线程拷贝只是搬内存，
   不会在持锁期间触发 string 的分配/析构。
2. **菜单的 bool 和线程间共享的变量之间永远隔一层快照**。菜单写 `菜单开关`（普通 bool），
   主线程每帧开头 `开关.store(菜单开关)`，渲染线程只读 `开关`（atomic）。
   逐项开关同理：主线程每帧 `memcpy` 一份到本地数组，整帧只用那份。
   — ImGui 要 `bool*`，绑不了 atomic，所以这层快照不是洁癖，是必须的。

---

## 2. 依赖清单

移植到你项目里，必须先有这些等价物。**没有的话按「替代方案」那列自己接**。

| 本项目的符号 | 作用 | 替代方案 |
|---|---|---|
| `UnityResolve::UnityType::Transform / Rigidbody / Component / Vector3` | IL2CPP 类型封装 | 你项目自己的 IL2CPP 封装。只需要这些能力：`Transform::GetPosition()`、`Rigidbody::GetVelocity()/SetVelocity()/GetDetectCollisions()/SetDetectCollisions()`、`Component::GetGameObject()/GetTransform()` |
| `UnityResolve::Class::FindObjectsByType<T>()` | 枚举场上某个类的全部实例 | 没有就退化成「枚举 Transform 再按名字筛」——**能跑，但会满屏误报**，理由见 §7.1 |
| `UnityResolve::assembly`（程序集列表） | 全程序集找类 | 见 §7.2，**千万别写死程序集名** |
| `读实体托管字符串(uintptr_t)` | 读 IL2CPP 托管 string → `std::string` | 你项目里读 GameObject 名字的那套 |
| `获取魔法目标骨骼位置(Vector3&)` | 读当前魔法子弹/自瞄目标的头部骨骼世界坐标 | **追踪的产物指向它**。等价物就是「当前锁定的敌人头部坐标」。注意它是**跨线程发布**的（存的是地址+有效性标记，不是现读） |
| `本人数据::本人坐标` | 本地玩家世界坐标 | 你项目的本地玩家坐标 |
| `本人数据::魔法目标地址 / 魔法目标有效 / 魔法目标是人机` | 魔法目标的跨线程快照 | 同上 |
| `掩体主线程钩子` | 挂 `CameraController.Update/LateUpdate` 的主线程每帧钩子 | **任何能保证在游戏主线程每帧跑一次的地方**（本项目用的是硬断点） |
| `自瞄::自瞄热键` | `追踪需按键` 用 | 你的自瞄热键，只在勾了「需按键」时才用到 |
| ImGui + `获取矩阵()` / `D3D转2D坐标` / `WorldToScreenBox` / `绘制ESP标签` / `绘制直线` / `绘制圆形` | 绘制 | 你项目的绘制原语。**这几个只影响显示，不影响追踪逻辑** |

依赖最多的其实是「当前目标头部坐标」这一格 —— 追踪整个功能就是「读目标头 → 把雷朝那儿拐」。
如果你项目还没有自瞄/目标选择，先把那个做出来，追踪是它的下游。

---

## 3. 头文件部分

放你自己的头文件里（本项目是 `Other.h`）。`ImU32` 来自 ImGui，不用 ImGui 就换成 `uint32_t`。

```cpp
// ============================ 投掷物预警 ============================
//
// 飞在空中的雷是一组三个 GameObject，位置完全相同：
//   · `Grenade`        —— Transform SphereCollider ObjCollisionEnter Rigidbody ← 物理本体
//   · `Grenade_tuowei` —— Transform TrailRenderer（拖尾）
//   · `FragGrenade`    —— Transform MeshFilter MeshRenderer（网格）
// 名字的「主词」就是配置名：手雷=Grenade、云雾弹=SmokeShell、爆燃弹=FireGrenade …，
// 拖尾统一是 `<主词>_tuowei`。
//
// **识别判据不能只看名字**：训练场武器台上摆着几个**同名但静止的展示件**
// （FragGrenade / SmokeGrenade / GrenadeMax / PinkSmokeShell，组件只有
// MeshFilter MeshRenderer TweenPosition，没有 Rigidbody），地上的物资又都带 (Clone)
// 后缀且挂着 PickItem。**唯一干净的分界是 Rigidbody** —— 飞着的那颗才有。
// 所以这里直接枚举 Rigidbody 实例再按名字筛，而不是枚举 Transform 再按名字筛。
//
// 落点自己算：每帧读它的世界坐标，帧间差分得速度、再差分得竖直加速度（实测 g，
// 不去猜游戏的物理参数），然后解 y(t) = y0 + vy·t − g·t²/2 落到地面高度的 t。
//
// 跨线程：**枚举和读坐标必须在游戏主线程**（IL2CPP 调用），由主线程钩子驱动；
// 外绘线程只读一份锁保护好的快照。两边各拿各的，绝不在持锁时画图。
namespace 投掷物预警 {
    // 一颗飞行中的投掷物，给外绘线程看的最终结果。**全 POD**，这样跨线程拷贝
    // 只是搬内存，不会在持锁期间碰 std::string 的分配与析构。
    struct 雷达 {
        glm::vec3 当前{};      // 世界坐标
        glm::vec3 落点{};      // 推算落点；没解出来时与 当前 相同
        float 距我 = 0.0f;     // 当前离本人多少米
        float 落点距我 = 0.0f; // 落点离本人多少米
        float 余秒 = -1.0f;    // 还有多久落地；-1 = 没解出
        int   类别 = 0;        // 见 雷类别，只决定颜色
        bool  落点有效 = false;
        // 这一颗正被投掷物追踪拐着走。只影响标签怎么写，画法不变。
        bool  被追踪 = false;
        // 这一种在「逐项开关」里是开着的。false 就整个不画 —— 注意**不影响追踪**：
        // 关掉一种预警只是屏幕上不出现它，那颗雷该拐弯还是拐。
        bool  预警开 = true;
        // 中文名在主线程查表时一次算好塞进来。外绘线程只拿快照，不再查表。
        char  名[24] = {};
    };

    // 类别只决定颜色；文字用雷达里那个中文名。认不出来的一律 类别_其他。
    enum 雷类别 {
        类别_手雷 = 0,    // 爆炸伤害
        类别_云雾弹 = 1,  // 遮蔽视野
        类别_爆燃弹 = 2,  // 火
        类别_治疗弹 = 3,
        类别_特殊 = 4,    // 位移 / 机关类
        类别_技能弹 = 5,  // 身份卡技能 / 模式专属弹道（奥特曼光线那一类）
        类别_其他 = 6,    // 认不出来
    };

    // ---- 投掷物名表 ----
    //
    // 表放在头文件里（而不是藏在 .cpp 的匿名 namespace）是为了让菜单能直接把它
    // 列出来做逐项开关。物件名 = 场景里那个 GameObject 的名字。
    //
    // **三个来源，都取自游戏自己的配置表，中文名是抄的不是编的**：
    //   · ItemTableBase.txt 的 res_sign 列：item_type_id 31(投掷物) 全部 14 个；
    //     26 里只有爆燃弹、46 里只有爆破炸弹是扔的（其余是子弹和药，
    //     **别按 item_type_id 抓**，会把几百发子弹全拉进来）。
    //   · BombLineRender.txt 的 SkillSign 列：官方那张「会走抛物线的东西」表，
    //     身份卡技能、奥特曼光线、球球模式技能都在里面。
    //   · GoldDashGameRoleSkill.txt / PartyHeroInfoConfig.txt：上表里那些英文标识
    //     对应的中文名 —— 技能说明里写着「投掷型」的那几个才是扔的，
    //     「瞬发型/持续型/状态型」（烟雾装置、无畏冲锋、冥王潜行…）**不是**，没收。
    //
    // 认不出来的在这里加一行就行。**别改成模糊匹配**（比如名字里含 "Bomb"），
    // 场景里 LD_ThrowBomb 有 37 个静态摆件，一放就满屏。
    // 真正挡噪音的是「必须挂着 Rigidbody 且在动」，见 主线程采样。
    struct 雷种 {
        const char* 物件名;
        const char* 中文名;
        int 类别;
    };
    inline const 雷种 雷种表[] = {
        // —— 爆炸伤害 ——
        { "Grenade",                 "手雷",             类别_手雷 },
        { "GrenadeMax",              "手雷Max",          类别_手雷 },
        { "De_C4",                   "爆破炸弹",         类别_手雷 },
        { "SandGrenade",             "沙雷",             类别_手雷 },
        { "AssaultClownGrenade",     "跳跳炸弹",         类别_手雷 },
        // —— 遮蔽视野 ——
        { "SmokeShell",              "云雾弹",           类别_云雾弹 },
        { "PinkSmokeShell",          "爱心云雾弹",       类别_云雾弹 },
        { "InkOcclusion",            "雪球",             类别_云雾弹 },
        { "ExpressionInkOcclusion",  "特质污污弹",       类别_云雾弹 },
        { "SnowGirlBall",            "雪女雪球",         类别_云雾弹 },
        { "BladeBall_SnowBall",      "球球雪球",         类别_云雾弹 },
        { "RainbowCloud",            "彩虹云雾",         类别_云雾弹 },
        // —— 火 ——
        { "FireGrenade",             "爆燃弹",           类别_爆燃弹 },
        { "FireBall",                "火球",             类别_爆燃弹 },
        { "SungodBurn",              "太阳神灼烧",       类别_爆燃弹 },
        { "AssaultSungodBurn",       "天火",             类别_爆燃弹 },
        // —— 治疗 ——
        { "DiscoBomb",               "治疗弹",           类别_治疗弹 },
        { "AidMeiHealingBot",        "甜心治疗机器人",   类别_治疗弹 },
        { "SupperAidMeiHealingBot",  "疗愈力场",         类别_治疗弹 },
        // —— 位移 / 机关 ——
        { "ShiftDevice",             "传送手雷",         类别_特殊 },
        { "PirateCannon",            "传送大炮",         类别_特殊 },
        { "SandCastle",              "战术掩体",         类别_特殊 },
        { "ShipAnchor",              "拉钩钩",           类别_特殊 },
        { "SlimeBomb",               "啵啵",             类别_特殊 },
        { "Lego",                    "积木",             类别_特殊 },
        { "RainbowBridge",           "彩虹桥",           类别_特殊 },
        { "CaoCaoShieldPawn",        "曹操盾牌兵",       类别_特殊 },
        { "SupperCaoCaoShieldPawn",  "超级盾牌兵",       类别_特殊 },
        { "ZhuGeLiangArrowTowers",   "军师箭塔",         类别_特殊 },
        { "ReconArrowTowers",        "侦察箭塔",         类别_特殊 },
        { "NinjaHookFly",            "忍者钩索",         类别_特殊 },
        { "NinjaHookFly_Skin1",      "忍者钩索·皮肤",    类别_特殊 },
        { "PaoKuFlyAir",             "球球飞天",         类别_特殊 },
        { "BladeBall_ShieldSoldier", "球球盾兵",         类别_特殊 },
        { "Zeta_Blink",              "赛塔闪现",         类别_特殊 },
        { "Adsorb",                  "吸附",             类别_特殊 },
        // —— 身份卡技能 / 模式专属弹道 ——
        { "GutsAirStrike",           "空袭",             类别_技能弹 },
        { "UltraFightGutsAirStrike", "终极空袭",         类别_技能弹 },
        { "Doctorwhoskill1",         "魔法师技能一",     类别_技能弹 },
        { "Doctorwhoskill2",         "魔法师技能二",     类别_技能弹 },
        { "Tiga_ZepellionRay",       "迪迦·泽佩利敖光线", 类别_技能弹 },
        { "Zeta_Shot",               "赛塔·射击",        类别_技能弹 },
        { "Zero_Emerium",            "赛罗·艾梅利姆",    类别_技能弹 },
        { "Zero_Wideshot",           "赛罗·宽幅射击",    类别_技能弹 },
        { "Geed_Shot",               "捷德·射击",        类别_技能弹 },
        { "Ultraman_Shot",           "奥特曼·射击",      类别_技能弹 },
        { "Ultraman_PuppetShot",     "奥特曼·傀儡射击",  类别_技能弹 },
        { "Monster_Shot",            "怪兽·射击",        类别_技能弹 },
        { "Monster_PuppetShot",      "怪兽·傀儡射击",    类别_技能弹 },
    };
    inline constexpr int 雷种数 = static_cast<int>(sizeof(雷种表) / sizeof(雷种表[0]));

    // 每条投掷物一个预警开关，**默认全开**。
    //
    // 包一层结构体是因为内置数组没法直接初始化成「全 true」，而这个默认值必须是
    // true —— 否则以后每加一个条目都会「静默不预警」，排查起来和「名字写错了」
    // 长得一模一样。菜单直接绑 &预警开关.开[i]（ImGui 要 bool*，绑不了原子）。
    struct 预警开关组 {
        bool 开[雷种数];
        预警开关组() { for (int i = 0; i < 雷种数; ++i) 开[i] = true; }
    };
    inline 预警开关组 预警开关;

    // 菜单直接绑这个（ImGui 要 bool*，绑不了 atomic）。
    inline bool 菜单开关 = false;
    // 跨线程用的那份：**只由主线程写**（每帧开头的 store），外绘线程只读。
    inline std::atomic<bool> 开关{ false };
    // 菜单上显示「这一拍看到几颗」，不用开日志就能判断有没有生效。
    inline std::atomic<int> 可见颗数{ 0 };

    // 主线程回报的运行状态。存在的意义是「查类失败」这一类问题是**静默**的 ——
    // 第一版写死了程序集名，Get("Rigidbody") 返回空，之后每层判空返回，
    // 现象就是勾了没反应、却不报任何错。有这一格就能一眼分开
    //「功能没跑起来」和「跑了但场上真没雷」。
    enum 运行状态 {
        状态_未运行 = 0,
        状态_正常 = 1,
        状态_找不到刚体类 = 2,
    };
    inline std::atomic<int> 诊断状态{ 状态_未运行 };

    // 「小心手雷」大字警告：落点离自己 ≤ 警告半径 米、且 ≤ 警告提前秒 秒内落地时，
    // 屏幕正中偏上弹一行大字。默认开 —— 预警的核心价值就在这一下。
    inline bool 警告开关 = true;
    inline float 警告半径 = 12.0f;
    inline float 警告提前秒 = 8.0f;

    // ---- 投掷物追踪 ----
    //
    // 「和魔法子弹一样」具体一样在哪：同一个触发键、同一个目标（读头部骨骼）、
    // 同一个时机（东西还没出去就已经指向他）。唯一不同的是执行方式 ——
    // 子弹开火那一瞬间把初速改掉就完事，而雷出手之后还要飞好几秒，
    // 弹道在出手时就定死了（Rpc_ThreadGrenade 里带着整条抛物线），
    // 所以这里改成**飞行途中每帧改写刚体速度**，让它变成一颗会拐弯的雷。
    //
    // 这条路走不走得通，取决于这颗雷是不是真由刚体物理驱动（见 物理速度读数）。
    inline bool 追踪开关 = false;
    // 跨线程那份：只由主线程写，外绘线程只读。和 开关 一个套路。
    inline std::atomic<bool> 追踪原子{ false };
    // 拐弯时的飞行速度（m/s）。太快会一帧穿过掩体，太慢追不上跑动的人。
    inline float 追踪速度 = 20.0f;
    // 无视掩体：追踪期间把雷的碰撞检测关掉，墙就挡不住它了。
    // 默认开 —— 「追着一个钻进掩体后面的人被墙弹回来」是这套功能最容易废掉的情形。
    inline bool 追踪无视掩体 = true;
    // 需不需要按住自瞄键才开始锁。**默认不要** —— 「扔出去就触发」，
    // 只要场上读得到魔法目标，一扔出去就锁上追过去。勾上才回到手动模式。
    inline bool 追踪需按键 = false;

    enum 追踪运行状态 {
        追踪_未启用 = 0,
        追踪_待锁定 = 1,   // 开着，但还没锁上：没按住键 / 附近没有刚出手的雷 / 没有目标
        追踪_锁定 = 2,
        追踪_目标无效 = 3, // 锁着，但魔法目标这一刻读不到（丢视野、或者人死了）
        追踪_命中 = 4,     // 已经贴到目标身上悬停，等引信烧完
        追踪_异常 = 5,     // 读写刚体时炸了，通常意味着这颗雷已经被回收
    };
    inline std::atomic<int> 追踪诊断{ 追踪_未启用 };

    // 给菜单看的一对数，用来判断「这颗雷到底归不归刚体物理管」——
    // 两个都在 追踪速度 附近 → 物理驱动，成了；
    // 物理速度是我们写的、实测速度却还在走原来的抛物线 → 位置是别处写的，这条路走不通。
    // 没有这一对数，两种情况的屏幕表现一模一样（雷不为所动），根本分不清。
    inline std::atomic<float> 物理速度读数{ 0.0f };
    inline std::atomic<float> 实测速度读数{ 0.0f };

    // 追踪期间**离目标最近到过多少米**（每锁一颗新雷就归零重记）。
    //
    // 这一格是用来把「受掩体影响」这句话变成事实的 —— 三种情况在屏幕上都是
    // 「雷没炸到人」，只有这个数分得开：
    //   收敛到 0.x 米   → 雷真追到人身上了，掩体没拦住，问题在别处；
    //   卡在某个整数米不动 → 地形真把它挡在中间，而且挡在哪一段一目了然；
    //   一直是 -1       → 这颗雷压根没进过追踪分支（闩没锁上），跟掩体无关。
    inline std::atomic<float> 追踪最近距目标{ -1.0f };

    inline std::mutex 锁;
    inline std::vector<雷达> 快照;

    // 主线程：枚举 + 采样 + 算落点，结果塞进 快照。由主线程钩子每帧调用。
    void 主线程采样();
    // 外绘线程：把 快照 里的落点投影到屏幕画出来。
    void 绘制();
    // 名字 → 类别。认不出的返回 类别_其他。
    int 认雷类别(const std::string& 名);
    // 名字 → 中文名。认不出的返回空指针。
    const char* 取雷中文名(const std::string& 名);
    // 类别 → 颜色。菜单的逐项开关列表也要用（画每行前面那个色点），
    // 所以它是这里的成员，不是 .cpp 匿名 namespace 里的静态函数。
    ImU32 取类别颜色(int 类别);
    // 这个名字像不像飞行投掷物的**本体**（不是拖尾、不是网格）。
    bool 像雷本体(const std::string& 名);
}
```

---

## 4. 实现部分（.cpp）

以下都在匿名 `namespace { ... }` 里，除了标了 `投掷物预警::` 的那几个。

### 4.1 查表 + 去后缀

```cpp
namespace {
    // 名表在头文件的 投掷物预警 namespace 里 —— 菜单要遍历它做逐项开关，
    // 藏在匿名 namespace 里就看不见了。这里只剩查表。

    // 查表。净名（已去 Clone 后缀）传进来。返回下标，-1 表示不是投掷物。
    // **返回下标而不是指针**：调用方要用这个下标去翻 预警开关，只拿指针还得再算一次。
    int 找雷种下标(const std::string& 净名)
    {
        for (int i = 0; i < 投掷物预警::雷种数; ++i)
            if (净名 == 投掷物预警::雷种表[i].物件名) return i;
        return -1;
    }

    // 去掉 Unity 实例化时自动加的 " (Clone)" 后缀。训练场里飞着的雷不带这个后缀，
    // 但别的场景未必，两种都认才不至于换个地图就整个失效。
    std::string 去克隆后缀(const std::string& 名)
    {
        const std::string 后缀 = " (Clone)";
        if (名.size() > 后缀.size()
            && 名.compare(名.size() - 后缀.size(), 后缀.size(), 后缀) == 0)
            return 名.substr(0, 名.size() - 后缀.size());
        return 名;
    }
```

### 4.2 跨帧状态 `雷跟踪项`

```cpp
    // 一颗雷的跨帧状态。
    struct 雷跟踪项 {
        glm::vec3 上一坐标{};
        float 上次采样时刻 = 0.0f;
        // 帧间差分出来的速度带浮点噪声（世界坐标在千米量级，float 精度只有 1e-4），
        // 直接拿去二次差分估重力会算出 ±3 m/s² 的抖动，所以过一次指数滑动平均。
        glm::vec3 平滑速度{};
        bool 平滑有效 = false;
        // 估重力用的基准：拿相隔 ≥0.15s 的两段平滑速度做差分。
        glm::vec3 重力基准速度{};
        float 重力基准时刻 = 0.0f;
        float 实测重力 = 0.0f;
        // 解出来的落点也会抖（它是由带噪声的速度算出来的），再过一次同样的滑动平均。
        // 圈在地上跳来跳去比圈画得不准更难看。
        glm::vec3 平滑落点{};
        bool 落点有值 = false;
        // 「确认在飞」是**一次性置位、之后一直是**的：只要测到过一次超过阈值的地速，
        // 这颗雷就永久算「在飞的」，直到它从刚体枚举里消失。不能每帧拿瞬时速度去判，
        // 那样抛物线顶点附近会一帧报一帧不报，屏幕上就是一闪一闪。
        bool 确认在飞 = false;
        int 类别 = 投掷物预警::类别_其他;
        char 中文名[24] = {};
        // 这一种有没有被用户在「逐项开关」里关掉。只影响画不画，
        // 不影响追踪 —— 见 主线程采样 里赋值那处的说明。
        bool 预警开 = true;
        // 本轮枚举有没有再看到它。没看到的下次枚举就清掉 —— 雷一炸对象就被回收，
        // 留着只会让这个指针哪天被复用后算出一串荒唐的速度。
        bool 本轮命中 = false;

        // ---- 追踪用 ----
        // 这颗雷的刚体组件。枚举时顺手存下来：追踪每帧都要拿它写速度，
        // 等到要用的时候再去 GetComponent 就是多一次原生调用，还可能拿不到。
        UnityResolve::UnityType::Rigidbody* 刚体 = nullptr;
        // 第一次被枚举到的时刻。判「是不是我刚扔的」靠它，不靠距离 ——
        // 扔出去的雷就生在本人身上、之后越飞越远，而别人扔来躺你脚边还没炸的那颗
        // 距离反而更近。只看距离会认错人。
        float 首次见到时刻 = 0.0f;
        // 这一拍的世界坐标。平滑速度是在「位移够大」时才更新的，追踪要拿**当前**
        // 坐标算朝向，不能拿 上一坐标 顶替。
        glm::vec3 本轮位置{};
        bool 追踪中 = false;
        // 追过就不再追第二次。正常流程用不到（追到炸为止，不主动撒手），
        // 只有读刚体炸了的时候用：那颗雷多半已经没了，不该再拿这个指针去试。
        bool 追过 = false;
        // 目标从哪一刻起读不到了。短暂丢视野（人钻到掩体后面）不该放手，
        // 但目标真死了、这个闩就会一直挂在这颗雷上，挡住下一颗 —— 所以超时要放。
        float 目标丢失起始 = 0.0f;
        // 碰撞检测被我们关掉过没有，以及关之前是什么值。**任何接管的游戏值都要记原值**，
        // 这颗雷的 GameObject 是会回池复用的（C_Ability_Base.CreateEntityToPool），
        // 关掉不还回去的话，下一颗从池子里拿出来的雷就会穿地板掉下去。
        bool 碰撞已关 = false;
        bool 原碰撞 = true;
        // 这一颗追到过的最小距离。**每颗雷自己记一份**：全局那一个会被下一颗覆盖，
        // 而「上一颗到底追上没有」往往才是要看的那条信息。
        float 最近距目标 = -1.0f;
    };

    // 全局学到过的重力。单颗雷飞得短、采样少，估出来的值噪声大；一旦有哪颗雷
    // 给出过一个合理值，后面解落点就都用它，比每颗各估各的稳。
    float 学习到的重力 = 0.0f;
```

### 4.3 全程序集找类

```cpp
    // 按类名在所有已加载程序集里找类，找到就缓存。
    //
    // **绝对不要写死程序集名**。Rigidbody 不在 UnityEngine.CoreModule.dll 里，它在
    // UnityEngine.PhysicsModule.dll（dump 里 [NativeHeader("Modules/Physics/Rigidbody.h")]
    // 那行就是证据）。第一版就是写死了 CoreModule，Get("Rigidbody") 返回空指针，
    // 后面每一层都老老实实判空返回 —— 于是功能**静默地什么都不做**，菜单勾了没反应、
    // 没有任何报错，从现象根本反推不到是查类失败。engine 类型分散在几十个
    // UnityEngine.*Module.dll 里，名字背不过来，只能扫。
    UnityResolve::Class* 全程序集找类(const char* 类名)
    {
        static std::unordered_map<std::string, UnityResolve::Class*> 缓存;
        const std::string 键 = 类名;

        const auto 命中 = 缓存.find(键);
        if (命中 != 缓存.end()) return 命中->second;

        // 先只扫 UnityEngine.* 的引擎模块，没有再全量兜底 —— 同名类在业务程序集里
        // 也可能有，先命中引擎那份才不会认错。
        UnityResolve::Class* 结果 = nullptr;
        for (int 轮次 = 0; 轮次 < 2 && !结果; ++轮次) {
            for (auto* 程序集 : UnityResolve::assembly) {
                if (!程序集) continue;
                if (轮次 == 0 && 程序集->name.rfind("UnityEngine", 0) != 0) continue;
                if (auto* 类 = 程序集->Get(类名)) { 结果 = 类; break; }
            }
        }

        // 只缓存找到的：程序集是 Init 时一次性枚举完的，理论上不会后补，
        // 但拿不准的事就别缓存死，免得一次早调用把结果永久钉成「没有」。
        if (结果) 缓存.emplace(键, 结果);
        return 结果;
    }
```

### 4.4 追踪三件套

```cpp
    // ---- 投掷物追踪：挑目标 + 执行 ----
    //
    // 触发键和目标都照抄魔法子弹，只有「怎么让它拐过去」这一件事不一样：
    // 不改初速，改成每帧改写刚体速度。
    //
    // 两条约定：
    //   ①**按下键才开始追，松开键继续追**。子弹是一瞬间的事，按住键开火天经地义；
    //     雷要在天上飞好几秒，要求全程按住键就太别扭了。
    //   ②**一次只追一颗，锁上就不换**。每帧重新挑的话，两颗雷都在附近时会出现
    //     这颗飞两下、那颗飞两下，两颗都拐不到位。

    // 关 / 开一颗雷的碰撞检测。关的时候先记原值，开的时候还回去。
    //
    // 关掉 detectCollisions 就是「穿墙」：Unity 里刚体的碰撞检测一关，
    // 墙壁、地形、别的刚体全都不再拦它，但**它自己还在动**（速度照常积分），
    // 所以是穿过去而不是卡在原地 —— 正是追一个钻进掩体后面的人要的效果。
    //
    // 注意它同时也会掐掉撞击类事件（雷身上那个 ObjCollisionEnter）。撞击引爆的雷
    // 会因此不炸，但每颗雷都还有引信（AbilityM_Grenade.M_LifeTime），到点照样爆。
    void 设定雷碰撞(雷跟踪项& 项, bool 关)
    {
        if (!项.刚体) return;
        if (关) {
            if (项.碰撞已关) return;
            项.原碰撞 = 项.刚体->GetDetectCollisions();
            项.刚体->SetDetectCollisions(false);
            项.碰撞已关 = true;
        }
        else {
            if (!项.碰撞已关) return;
            项.刚体->SetDetectCollisions(项.原碰撞);
            项.碰撞已关 = false;
        }
    }

    // 还碰撞，自带 SEH。用在**追踪已经结束**的时候 —— 那时候这颗雷多半已经炸了、
    // 指针是死的，直接调 SetDetectCollisions 有炸掉整个主线程的风险。
    // 不管成没成都把标志清掉：标志留着只会让它每帧白试一次。
    void 安全还碰撞(雷跟踪项& 项)
    {
        if (!项.碰撞已关) return;
        const bool 原值 = 项.原碰撞;
        auto* 刚体 = 项.刚体;
        项.碰撞已关 = false;
        if (!刚体) return;
        __try {
            刚体->SetDetectCollisions(原值);
        }
        __except (EXCEPTION_EXECUTE_HANDLER) {
        }
    }

    float 取追踪速度()
    {
        // 每次现取，不缓存：菜单里拖完滑块要立刻生效。
        float 速度 = 投掷物预警::追踪速度;
        if (!std::isfinite(速度)) 速度 = 20.0f;
        if (速度 < 1.0f) 速度 = 1.0f;
        if (速度 > 80.0f) 速度 = 80.0f;
        return 速度;
    }

    // 这一拍追哪颗。返回 nullptr = 不追。
    雷跟踪项* 选出追踪目标(
        std::unordered_map<UnityResolve::UnityType::Transform*, 雷跟踪项>& 跟踪, float 现在)
    {
        if (!投掷物预警::追踪原子.load(std::memory_order_relaxed)) {
            // 关掉时把闩全解开：留着的话下次打开一按键就锁到一颗几分钟前的陈雷上。
            // 顺手把关掉的碰撞还回去 —— 不还的话这颗雷会一路穿地板掉下去，
            // 用户看到的现象是「一关追踪雷就掉出地图」，完全联想不到是这里。
            for (auto& 对 : 跟踪) {
                对.second.追踪中 = false;
                设定雷碰撞(对.second, false);
            }
            投掷物预警::追踪诊断.store(投掷物预警::追踪_未启用, std::memory_order_relaxed);
            return nullptr;
        }

        // 已经锁着的那颗优先，一直追到它从场上消失为止。
        for (auto& 对 : 跟踪)
            if (对.second.追踪中) return &对.second;

        // 什么时候**开始**锁。默认不看键 —— 扔出去就触发，只要场上有魔法目标。
        // 勾了「需按键」才回到老行为：按住自瞄键扔，松手之后由上面那一段接着追。
        //
        // 不看键这件事之所以还能算安全，是因为下面的候选条件已经很紧了：
        // 必须是「确认在飞」、首见 3 秒内、且出手点离本人 12 米以内 —— 也就是
        // 基本只可能是自己刚扔出去的那一颗。别人的雷要满足这三条，得贴着你的脸扔。
        if (投掷物预警::追踪需按键 && GetAsyncKeyState(自瞄::自瞄热键) == 0) {
            投掷物预警::追踪诊断.store(投掷物预警::追踪_待锁定, std::memory_order_relaxed);
            return nullptr;
        }

        // 追的就是魔法子弹当前那个目标 —— 枪打谁雷就砸谁，不用再选一次。
        UnityResolve::UnityType::Vector3 骨骼{};
        if (!获取魔法目标骨骼位置(骨骼)) {
            投掷物预警::追踪诊断.store(投掷物预警::追踪_目标无效, std::memory_order_relaxed);
            return nullptr;
        }

        const glm::vec3 本人 = 本人数据::本人坐标;

        雷跟踪项* 最好 = nullptr;
        for (auto& 对 : 跟踪) {
            雷跟踪项& 项 = 对.second;
            if (项.追踪中 || 项.追过 || !项.确认在飞 || !项.刚体) continue;
            // 只认刚出手 3 秒内的。别人扔过来躺你脚边的老雷不该被接管 ——
            // 把它拐向目标等于替对面把雷捡起来再扔一次，不是我们要的。
            if (现在 - 项.首次见到时刻 > 3.0f) continue;
            // 出手那一刻雷就生在本人身上，12 米已经飞出去很远了，不该还算「我的」。
            if (glm::length(项.本轮位置 - 本人) > 12.0f) continue;
            if (!最好 || 项.首次见到时刻 > 最好->首次见到时刻) 最好 = &项;
        }

        if (最好) {
            最好->追踪中 = true;
            // 新锁一颗就把「最近到过多少米」清零重记，否则读数会是上一颗的成绩。
            最好->最近距目标 = -1.0f;
            投掷物预警::追踪最近距目标.store(-1.0f, std::memory_order_relaxed);
        }
        投掷物预警::追踪诊断.store(
            最好 ? 投掷物预警::追踪_锁定 : 投掷物预警::追踪_待锁定, std::memory_order_relaxed);
        return 最好;
    }

    // 把一颗雷拐向目标。每帧一次，位置传**这一拍**的坐标。
    void 执行追踪(雷跟踪项& 项, UnityResolve::UnityType::Transform* 变换, const glm::vec3& 位置)
    {
        const float 速度 = 取追踪速度();
        // 实测速度是位置差分出来的，和 物理速度读数 一对照就知道速度写没写进去。
        投掷物预警::实测速度读数.store(glm::length(项.平滑速度), std::memory_order_relaxed);

        if (!项.刚体) return;

        const float 现在 = static_cast<float>(GetTickCount64()) * 0.001f;

        // 下面全是 IL2CPP 原生调用，雷在这中间被回收掉是有可能的，走 SEH。
        __try {
            UnityResolve::UnityType::Vector3 骨骼{};
            if (!获取魔法目标骨骼位置(骨骼)) {
                投掷物预警::追踪诊断.store(投掷物预警::追踪_目标无效, std::memory_order_relaxed);
                // 没目标可追就别让它继续穿墙飞了，还回正常碰撞。
                设定雷碰撞(项, false);
                // 丢一下视野（人钻进掩体）不该放——他出来还得接着追。
                // 但目标真死了就永远读不到了，闩会一直挂在这颗雷上挡住下一颗，所以超时放。
                if (项.目标丢失起始 <= 0.0f) 项.目标丢失起始 = 现在;
                else if (现在 - 项.目标丢失起始 > 2.0f) {
                    项.追踪中 = false;
                    项.追过 = true;
                }
                return;
            }
            项.目标丢失起始 = 0.0f;

            const glm::vec3 差 = glm::vec3(骨骼) - 位置;
            const float 距目标 = glm::length(差);
            if (距目标 > 200.0f) {
                设定雷碰撞(项, false);
                return;
            }

            // 记「最近到过多少米」。放在 200 米那一道护栏**后面** ——
            // 目标传送到地图另一头读出来的那一帧是个荒唐值，不该被当成成绩记下来。
            if (项.最近距目标 < 0.0f || 距目标 < 项.最近距目标) {
                项.最近距目标 = 距目标;
                投掷物预警::追踪最近距目标.store(距目标, std::memory_order_relaxed);
            }

            // 掩体：**整段飞行都不还碰撞** —— 目标钻进掩体后面时，墙不能把雷弹回来，
            // 否则「追着一个躲墙后的人」正是这套功能最该管用的场景，却最先废掉。
            //
            // 这里原来留了个 3 米窗口（贴近了把碰撞还回去，想让最后一段撞上人引爆）。
            // 那是个 bug，而且正好卡在唯一要紧的场景上：**人贴墙站的时候，挡住他的
            // 那面墙本来就在他 3 米之内**。于是雷穿过远端那道墙、却被紧贴目标的
            // 掩体边缘挡下来 —— 现象就是「追踪是能用，但受掩体影响」。
            //
            // 还回去的两个理由现在都不成立：引爆不靠撞击，每颗雷都有引信；
            // 而「别把关闭状态漏给池子里复用的下一颗」由 安全还碰撞 在三条退出路径上兜着：
            // 目标无效、追到炸掉跟踪项被清、关掉追踪开关。
            设定雷碰撞(项, 投掷物预警::追踪无视掩体);

            // 贴到身上就不再动它，让它原地等引信烧完。
            if (距目标 < 0.5f) {
                项.刚体->SetVelocity(glm::vec3(0.0f));
                投掷物预警::追踪诊断.store(投掷物预警::追踪_命中, std::memory_order_relaxed);
                return;
            }

            // **一直追到它炸，中途不撒手。** 提前撒手的话，一颗还剩两秒引信的雷
            // 会落在目标脚边而不是身上，对面往旁边挪一步就白追了。
            //
            // 但贴近了必须减速：恒速 20m/s 冲过去会在目标周围来回穿越、绕着人画圆，
            // 照样炸不到。所以速度按距离收敛 —— 越近越慢，最后变成贴着目标悬停，
            // 一直吊到他身上等到引信烧完。
            float 这一段速度 = 速度;
            if (距目标 < 3.0f) 这一段速度 = (std::min)(这一段速度, 距目标 / 0.15f);

            const glm::vec3 方向 = 差 / 距目标;
            项.刚体->SetVelocity(方向 * 这一段速度);
            if (距目标 <= 2.0f)
                投掷物预警::追踪诊断.store(投掷物预警::追踪_命中, std::memory_order_relaxed);

            // 写进去再读回来。这是判断「这颗雷到底归不归刚体物理管」唯一直接的手段：
            // 刚体是 kinematic 的话 set_velocity 是空操作，这条路就永远不生效，
            // 得早点发现，别让人对着一个不动的雷反复怀疑是自己没扔准。
            投掷物预警::物理速度读数.store(
                glm::length(项.刚体->GetVelocity()), std::memory_order_relaxed);
        }
        __except (EXCEPTION_EXECUTE_HANDLER) {
            项.追踪中 = false;
            项.追过 = true;
            // 异常多半是这颗雷已经被回收了，所以**不在这里**碰那个指针，
            // 留给下一帧的 安全还碰撞（它自带 SEH，指针死了也只是白试一次）。
            投掷物预警::追踪诊断.store(投掷物预警::追踪_异常, std::memory_order_relaxed);
        }
    }
}  // namespace
```

### 4.5 名字 → 类别 / 中文名 / 颜色

```cpp
bool 投掷物预警::像雷本体(const std::string& 名)
{
    return 找雷种下标(去克隆后缀(名)) >= 0;
}

int 投掷物预警::认雷类别(const std::string& 名)
{
    const int 下标 = 找雷种下标(去克隆后缀(名));
    return 下标 >= 0 ? 雷种表[下标].类别 : 类别_其他;
}

const char* 投掷物预警::取雷中文名(const std::string& 名)
{
    const int 下标 = 找雷种下标(去克隆后缀(名));
    return 下标 >= 0 ? 雷种表[下标].中文名 : nullptr;
}

// 类别 → 颜色。ESP 圈、本体点、大字警告、二级页逐项开关列表四处共用同一套，
// 所以它必须是 投掷物预警 的成员而不是匿名 namespace 里的静态函数 ——
// 菜单要照着色点画那个小圆，调不到匿名 namespace 里的东西。
ImU32 投掷物预警::取类别颜色(int 类别)
{
    switch (类别) {
    case 类别_手雷:   return IM_COL32(255, 82, 82, 255);   // 红：炸你
    case 类别_云雾弹: return IM_COL32(120, 220, 255, 255); // 青：挡视线
    case 类别_爆燃弹: return IM_COL32(255, 156, 64, 255);  // 橙：火
    case 类别_治疗弹: return IM_COL32(130, 230, 140, 255); // 绿
    case 类别_特殊:   return IM_COL32(200, 140, 255, 255); // 紫：位移/机关
    case 类别_技能弹: return IM_COL32(255, 214, 92, 255);  // 金：身份卡技能/模式专属
    default:          return IM_COL32(235, 235, 235, 255); // 白
    }
}
```

### 4.6 主线程采样（核心）

```cpp
void 投掷物预警::主线程采样()
{
    static std::unordered_map<UnityResolve::UnityType::Transform*, 雷跟踪项> 跟踪;
    static float 上次枚举 = 0.0f;

    // 菜单那两个 bool 都是通过这里过到原子变量上的 —— 只有主线程写，外绘线程只读。
    开关.store(菜单开关, std::memory_order_relaxed);
    追踪原子.store(追踪开关, std::memory_order_relaxed);

    // 追踪单独开着也要跑采样：它要的正是「雷在哪、速度多少」这些数据。
    if (!开关.load(std::memory_order_relaxed) && !追踪原子.load(std::memory_order_relaxed)) {
        // 关掉时把跟踪一起清干净：留着的话，下次打开第一帧会拿几分钟前的旧坐标
        // 当「上一帧」，瞬间算出一个荒唐的速度。
        if (!跟踪.empty()) 跟踪.clear();
        {
            std::lock_guard<std::mutex> 守卫(锁);
            if (!快照.empty()) 快照.clear();
        }
        可见颗数.store(0, std::memory_order_relaxed);
        诊断状态.store(状态_未运行, std::memory_order_relaxed);
        追踪诊断.store(追踪_未启用, std::memory_order_relaxed);
        return;
    }

    const float 现在 = static_cast<float>(GetTickCount64()) * 0.001f;

    // 逐项开关的快照。菜单在很多线程之外的绘制线程上写这几个 bool，采样在主线程读 ——
    // 一帧拷一次进本地数组，整帧只用这份，和 菜单开关→开关 那对原子变量是同一个套路。
    // 不拷也能跑（bool 读写不会撕裂），但那样这个数组就成了唯一没走快照的跨线程共享，
    // 以后加东西的人不会知道该不该动它。
    static bool 本帧开关[投掷物预警::雷种数];
    memcpy(本帧开关, 投掷物预警::预警开关.开, sizeof(本帧开关));

    // ---- 一、每 200ms 重新枚举一次场上挂着 Rigidbody 的雷 ----
    if (现在 - 上次枚举 >= 0.2f) {
        上次枚举 = 现在;
        for (auto& 项 : 跟踪) 项.second.本轮命中 = false;

        UnityResolve::Class* 刚体类 = 全程序集找类("Rigidbody");
        // 把「查类成没成」暴露到菜单上。这一层失败是静默的：拿不到类 → 枚举 0 个 →
        // 快照恒空 → 菜单勾了什么都不画，现象和「场上真没雷」一模一样。
        诊断状态.store(刚体类 ? 1 : 2, std::memory_order_relaxed);
        if (刚体类) {
            std::vector<UnityResolve::UnityType::Component*> 刚体们;
            try { 刚体们 = 刚体类->FindObjectsByType<UnityResolve::UnityType::Component*>(); }
            catch (...) { 刚体们.clear(); }

            for (auto* 刚体 : 刚体们) {
                if (!刚体) continue;
                // 逐个 try：对象可能在枚举和读取之间就被销毁了。
                try {
                    auto* 物体 = 刚体->GetGameObject();
                    if (!物体) continue;
                    auto* 托管名 = 物体->GetName();
                    if (!托管名) continue;
                    const std::string 名 = 读实体托管字符串(reinterpret_cast<uintptr_t>(托管名));
                    if (名.empty()) continue;
                    const int 雷种下标 = 找雷种下标(去克隆后缀(名));
                    if (雷种下标 < 0) continue;

                    auto* 变换 = 物体->GetTransform();
                    if (!变换) continue;

                    雷跟踪项& 项 = 跟踪[变换];
                    项.本轮命中 = true;
                    项.类别 = 投掷物预警::雷种表[雷种下标].类别;
                    // 逐项开关只挡**预警**（圈、标签、大字），不挡追踪 ——
                    // 「点名要预警哪几种」和「雷要不要拐弯」是两件事，
                    // 关掉「手雷」的预警不该让手雷突然不追踪了。
                    项.预警开 = 本帧开关[雷种下标];
                    std::snprintf(项.中文名, sizeof(项.中文名), "%s",
                        投掷物预警::雷种表[雷种下标].中文名);
                    // 刚体指针每轮重记一次：这一轮的对象可能已经是新实例
                    //（旧的那颗炸了、这个 GameObject 被池化后复用），旧指针写下去
                    // 就是往一个已经回收的对象上写速度。
                    项.刚体 = static_cast<UnityResolve::UnityType::Rigidbody*>(刚体);
                    if (项.首次见到时刻 <= 0.0f) 项.首次见到时刻 = 现在;
                }
                catch (...) {}
            }
        }

        for (auto 迭代 = 跟踪.begin(); 迭代 != 跟踪.end(); ) {
            if (!迭代->second.本轮命中) {
                // 这颗雷从场上消失了（炸了 / 被回收）。碰撞要是还关着就尽量还回去 ——
                // 它这个 GameObject 是回池子复用的，状态不改回来会漏给下一颗雷。
                安全还碰撞(迭代->second);
                迭代 = 跟踪.erase(迭代);
            }
            else ++迭代;
        }
    }

    // ---- 二、先定这一拍追哪颗雷 ----
    // 放在采样循环**之前**：这样循环里就能直接拿 项.追踪中 给快照打标记，
    // 当场生效，不用等下一帧。
    选出追踪目标(跟踪, 现在);

    // ---- 三、每帧采样，差分出速度，再差分出重力，最后解落点 ----
    const glm::vec3 本人 = 本人数据::本人坐标;
    std::vector<雷达> 结果;
    结果.reserve(跟踪.size());

    for (auto& 对 : 跟踪) {
        雷跟踪项& 项 = 对.second;

        D3D坐标 位置坐标{};
        try { 位置坐标 = 对.first->GetPosition(); }
        catch (...) { continue; }
        const glm::vec3 位置 = 位置坐标;

        if (!std::isfinite(位置.x) || !std::isfinite(位置.y) || !std::isfinite(位置.z)) continue;

        项.本轮位置 = 位置;

        // 追踪就放在这里：位置是这一拍刚读的，最准的时候。
        // 写完速度不影响本帧的读数，下一拍的位置才会体现出来。
        if (项.追踪中) 执行追踪(项, 对.first, 位置);
        // 这一拍没在追的（刚被放手的、追的过程中出事的），把碰撞还回去。
        else if (项.碰撞已关) 安全还碰撞(项);

        if (项.上次采样时刻 <= 0.0f) {
            // 第一次见到它：只记基准，这一帧还测不出速度。
            项.上一坐标 = 位置;
            项.上次采样时刻 = 现在;
        }
        else {
            const float 帧间隔 = 现在 - 项.上次采样时刻;
            const glm::vec3 位置差 = 位置 - 项.上一坐标;
            const float 位移 = glm::length(位置差);

            // 位移太小的帧不参与测速：世界坐标在千米量级，float 只有 ~1e-4 的分辨率，
            // 120fps 下除以 8ms 会放大成 0.01 m/s 量级的纯噪声，再二次差分就是
            // ±3 m/s² 的重力估计抖动。等它动够 2cm 再算。
            //
            // **注意这里只是「这一帧不更新速度」，绝不是把这一帧整条丢掉。**
            // 原来写的是 continue —— 于是「动够 / 没动够 / 动够」一路交替，
            // 这颗雷就会一帧出现在快照里、一帧消失，屏幕上那个点一闪一闪。
            if (位移 >= 0.02f && 帧间隔 > 0.0f && 帧间隔 <= 0.5f) {
                const glm::vec3 原始速度 = 位置差 / 帧间隔;
                const glm::vec3 速度 = 项.平滑有效
                    ? glm::mix(项.平滑速度, 原始速度, 0.25f)
                    : 原始速度;
                项.平滑速度 = 速度;
                项.平滑有效 = true;
                项.上一坐标 = 位置;
                项.上次采样时刻 = 现在;

                if (glm::length(速度) > 2.0f) 项.确认在飞 = true;

                // 重力：拿相隔 ≥0.15s 的两段平滑速度做差分，间隔太近全是噪声。
                if (项.重力基准时刻 <= 0.0f) {
                    项.重力基准速度 = 速度;
                    项.重力基准时刻 = 现在;
                }
                else if (现在 - 项.重力基准时刻 >= 0.15f) {
                    const float 跨度 = 现在 - 项.重力基准时刻;
                    const float 估重 = -(速度.y - 项.重力基准速度.y) / 跨度;
                    // 只认重力该有的量级。物体被回收/瞬移时会算出荒唐值，别让它污染解算。
                    if (估重 > 1.0f && 估重 < 30.0f) {
                        项.实测重力 = 估重;
                        学习到的重力 = 估重;
                    }
                    项.重力基准速度 = 速度;
                    项.重力基准时刻 = 现在;
                }
            }
        }

        // 一次速度都没测出来（刚生成）或者从没动过（地上的物件）就不报。
        // 注意判的是**置位过没有**，不是这一帧的速度 —— 见 确认在飞 的注释。
        if (!项.确认在飞) continue;

        雷达 条;
        条.当前 = 位置;
        条.类别 = 项.类别;
        条.被追踪 = 项.追踪中;
        条.预警开 = 项.预警开;
        std::snprintf(条.名, sizeof(条.名), "%s", 项.中文名);
        条.距我 = glm::length(位置 - 本人);
        条.落点 = 位置;
        条.落点距我 = 条.距我;

        // 解 y(t) = y0 + vy·t − g·t²/2 里落到地面的那个 t（取正根）。
        // 地面高度直接用本人脚下的 y —— 地形起伏的地方会有偏差，但只要雷冲你来，
        // 误差就在几米内，预警够用。真要做准得对地面做射线检测，先不引这个开销。
        // 重力优先用这颗雷实测的，其次用别处学到的，最后才退回配置表的 0.5×9.81。
        const float g = 项.实测重力 > 1.0f ? 项.实测重力
            : (学习到的重力 > 1.0f ? 学习到的重力 : 4.905f);
        const float 判别 = 项.平滑速度.y * 项.平滑速度.y + 2.0f * g * (位置.y - 本人.y);
        if (判别 >= 0.0f) {
            const float t = (项.平滑速度.y + std::sqrt(判别)) / g;
            if (t > 0.0f && t < 30.0f) {
                const glm::vec3 新落点(位置.x + 项.平滑速度.x * t, 本人.y, 位置.z + 项.平滑速度.z * t);
                项.平滑落点 = 项.落点有值 ? glm::mix(项.平滑落点, 新落点, 0.3f) : 新落点;
                项.落点有值 = true;
                条.落点 = 项.平滑落点;
                条.落点距我 = glm::length(条.落点 - 本人);
                条.余秒 = t;
                条.落点有效 = true;
            }
        }

        结果.push_back(条);
    }

    {
        std::lock_guard<std::mutex> 守卫(锁);
        快照.swap(结果);
    }
    可见颗数.store(static_cast<int>(快照.size()), std::memory_order_relaxed);
}
```

> ⚠️ 上面这版正负号按 `g > 0`（向下为负的 y 轴）写的。如果你项目的坐标系 y 向上为正，
> 解算那三行要自己推一遍，别直接抄：`判别 = vy² + 2g(y0 − y地面)`，`t = (vy + √判别)/g`。
> 重力估出来是正数、落点解出来比当前 y 小，就说明对上号了。

---

## 5. 接入点

### 5.1 主线程每帧（必须）

在你的主线程钩子里每帧调一次。本项目是这样接的：

```cpp
void 掩体主线程钩子::每帧钩子(void* _this)
{
    ...
    // 投掷物预警：开关关着时只读一个原子变量就返回，开着才枚举 Rigidbody。
    挂起看门狗::标记阶段(挂起看门狗::槽主线程, 9);
    投掷物预警带SEH();
    挂起看门狗::标记阶段(挂起看门狗::槽主线程, 0);

    HardBreakPoint::call_origin(每帧钩子, _this);
}
```

`投掷物预警带SEH()` 就是「调 `主线程采样()` 并包一层 SEH」。**这一层不能省** ——
主线程钩子没有任何外层 SEH，异常一旦逃逸就是整个进程崩溃：

```cpp
void 投掷物预警带SEH()
{
    __try { 投掷物预警::主线程采样(); }
    __except (EXCEPTION_EXECUTE_HANDLER) {}
}
```

### 5.2 绘制线程（可选）

`投掷物预警::绘制()` 的两个非显然点，抄的时候别漏：

- **落点圈要画在世界水平面上再逐点投影**，不能直接画屏幕空间的圆。屏幕圆永远是正圆，
  看着像贴在镜头上的一片贴纸；世界圆投影出来是贴着地皮的透视椭圆，斜着看会被压扁拉长，
  一眼能看出圈在哪。取 `(落点.x + R·cosθ, 落点.y, 落点.z + R·sinθ)` 一圈 40 个点，
  逐点 `D3D转2D坐标` 再连线；投影失败的那段**断开**，不要把画面横着拉一条线。
- 持锁只做一次 `本地 = 快照` 拷贝，**所有画图在锁外**。

### 5.3 配置持久化

至少存这几个（本项目写在 `MoBai.ini`）：

```
投掷物预警开关 / 投掷物追踪开关 / 投掷物追踪速度 / 投掷物追踪无视掩体
投掷物追踪需按键 / 投掷物警告开关 / 投掷物警告半径 / 投掷物警告提前秒
+ 每个 雷种表[i].物件名 一个逐项开关
```

逐项开关用物件名当 key（不是下标）—— 名字表以后加行不会串位。

---

## 6. 移植时最容易踩的坑

按「重新踩一遍的概率 × 排查难度」排序：

1. **别用 Transform 枚举，用 Rigidbody 枚举。** 场景里同名但静止的展示件到处都有，
   地上的物资又叫 `xxx (Clone)` 且挂 `PickItem`。飞着的那颗唯一干净的分界就是**挂着 Rigidbody**。
   顺带：刚体全场只有几十个，比扫上万个 Transform 便宜得多。
2. **别写死程序集名找类。** `Rigidbody` 在 `UnityEngine.PhysicsModule.dll`，
   不在 `CoreModule`。写错了 `Get()` 返回空 → 每层判空返回 → **静默什么都不做**，
   没有任何报错，现象和「场上真没雷」一模一样。所以弄一格 `诊断状态` 把「查类成没成」显示出来。
3. **关掉的碰撞一定要还。** 雷的 GameObject 会回池复用，不还原的话下一颗拿出来的会穿地板掉下去，
   现象是「一关追踪雷就掉出地图」，完全联想不到是这里。三条退出路径都要兜：
   目标无效、跟踪项被清、关掉开关。
4. **位移不够的那帧不要 `continue`。** 只跳过「更新速度」这一件事，整帧要继续往下走。
   写成 `continue` 会让「动够/没动够」交替，屏幕上的点一帧有一帧无，看起来在闪。
5. **「确认在飞」是一次性置位，不能每帧拿瞬时速度重判。** 否则抛物线顶点附近会一帧报一帧不报。
6. **别在贴近目标时把碰撞还回去。** 人贴墙站的时候，挡住他的那面墙本来就在他 3 米之内 ——
   还了碰撞正好卡死在唯一要紧的场景上。引爆不靠撞击，每颗雷都有引信。
7. **贴近了必须减速**（`距目标 < 3m` 时 `min(速度, 距目标/0.15)`）。恒速冲过去会在目标周围
   来回穿越、绕着人画圆，照样炸不到。
8. **一次只追一颗，锁上就不换。** 每帧重挑会让两颗附近的雷「这颗飞两下、那颗飞两下」，都拐不到位。
9. **所有 IL2CPP 原生调用包 SEH。** 雷随时可能被回收，指针是死的。
10. **追踪的失败是静默的。** 一定要留那几格诊断（`追踪诊断` / `物理速度读数` vs `实测速度读数` /
    `追踪最近距目标`），否则「雷没反应」这一个现象对应至少五种原因。
    `物理速度读数 ≈ 追踪速度` 是「刚体真被我们驱动了」的唯一直接证据。
